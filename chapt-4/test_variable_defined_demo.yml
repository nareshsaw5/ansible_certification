--- 
- name:  Test Variable is Defined Demo
  hosts:  prod
  vars:
     my_service:  httpd

  tasks:
  - name:  "{{ my_service }} package is enabled"
    ansible.builtin.service:
       name:  "{{ my_service }}"
       state:  started
       enabled:  true
    when:  my_service is defined
