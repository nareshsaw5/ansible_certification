---
- name:  Gather facts except hardware
  hosts: all
  gather_facts:  false
  tasks:
  - name:  Gather everything except hardware
    ansible.builtin.setup:
      gather_subset:
        - "!hardware"

