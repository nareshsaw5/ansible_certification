---
- name:  Gather subset of the facts
  hosts: all
  gather_facts:  false
  tasks:
  - name:  Gather subset fact - hardware
    ansible.builtin.setup:
      gather_subset:
        - hardware
  - name:  Print hardware
    ansible.builtin.debug:
      msg: >
        The hardware {{  ansible_facts }} 
